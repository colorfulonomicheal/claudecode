{
  "name": "claude-code-agent-env",
  "version": "1.0.0",
  "description": "Claude Codeを活用したAIエージェント業務自動化環境",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "lint": "prettier --check 'src/**/*.ts' 'examples/**/*.ts'",
    "format": "prettier --write 'src/**/*.ts' 'examples/**/*.ts'"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
